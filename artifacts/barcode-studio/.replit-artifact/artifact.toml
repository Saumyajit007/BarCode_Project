kind = "web"
previewPath = "/"
title = "Barcode Studio"
version = "1.0.0"
id = "artifacts/barcode-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24071

[services.development]
run = "pnpm --filter @workspace/barcode-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/barcode-studio", "run", "build" ]
publicDir = "artifacts/barcode-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24071"
BASE_PATH = "/"
